# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/fbpa?retryWrites=true&w=majority

# Authentication
JWT_SECRET=change-this-to-a-secure-random-string-min-32-characters
JWT_EXPIRES_IN=1h

# Server Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGIN=https://your-frontend-url.com,https://another-frontend.com

# Optional: Static file serving (set to 'true' if serving React build from this server)
SERVE_STATIC=false
