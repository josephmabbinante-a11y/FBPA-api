# Database Configuration
MMONGODB_URI=mongodb+srv://josephmabbinante_db_user:tEkZDundjpXeSa2G@cluster0.fvycshx.mongodb.net/?appName=Cluster0
# Authentication
JWT_SECRET=change-this-to-a-secure-random-string-min-32-characters
JWT_EXPIRES_IN=1h

# Server Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGIN=https://hdhtransport.com,https://express-git-fbpa-josephmabbinante-a11ys-projects.vercel.app

# Optional: Static file serving (set to 'true' if serving React build from this server)
SERVE_STATIC=false

# Optional: Email/SMTP Configuration (for sending notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@audit-iq.com
